services:
  - type: web
    name: moneywise-sync-backend
    runtime: docker
    rootDir: backend
    plan: starter
    healthCheckPath: /health
    disk:
      name: moneywise-backend-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8787
      - key: DATABASE_PATH
        value: /data/moneywise-sync.sqlite
      - key: MONEYWISE_BACKEND_AUTH_MODE
        value: password-only
      - key: MONEYWISE_BACKEND_SESSION_TTL_DAYS
        value: 30
      - key: AUTH_SECRET
        generateValue: true
      - key: MONEYWISE_TLS_TERMINATED
        value: true
      - key: MONEYWISE_BACKUP_DIRECTORY
        value: /data/backups
      - key: MONEYWISE_BACKUP_MAX_AGE_HOURS
        value: 24
      - key: MONEYWISE_BACKUP_INTERVAL_HOURS
        value: 24
